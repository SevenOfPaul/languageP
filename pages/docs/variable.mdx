import { Image,Callout } from 'nextra/components'
import Link from "next/link"

## 变量声明
### 试试你的hello world![^ps1]
+ 在你的编辑器中新建一个`hello.p`文件，输入
```javascript
print "hello world!";
```
<br/>
<Image src="/hello.png" className='w-full'/>
+ ~~恭喜，你学会了鹏语言，去放飞自我吧。~~，万里长征可以迈第二步了。 
<div class="chat chat-start">
  <div class="chat-bubble">
 去打印五个白色的`hello world!`。
  </div>
  </div>
  ```ts
print "hello world!";
print "hello world!";
print "hello world!";
print "hello world!";
print "hello world!";
```
<br/>
<div class="chat chat-end">
  <div class="chat-bubble">快来夸夸我！</div>
</div>
<div class="chat chat-start">
  <div class="chat-bubble">
 再去打印十个七彩斑斓`hello world!`。
  </div>
  </div>
  <div class="chat chat-end">
  <div class="chat-bubble">(╯°Д°)╯︵ ┻━┻ 老子不干了。</div>
</div>
<div class="chat chat-start">
  <div class="chat-bubble">
那如果有了变量呢
  </div>
  </div>
```ts
//鹏语言支持中文关键字
诏 字符="hello world!"
打印 字符;
打印 字符;
打印 字符;
打印 字符;
打印 字符;
```
<br/>
<div class="chat chat-start">
  <div class="chat-bubble">
  (↼_↼)有区别？
  </div>
</div>
<div class="chat chat-end">
  <div class="chat-bubble">别着急，要组合起来用的</div>
</div>

```ts
//伪代码
诏 字符="hello world!"
循环{
    打印 字符;
}
//这样就可以一直打印下去了
```
### 那我该怎么进行变量声明呢
+ 很简单
```ts
let str="hello world!"
//或
诏 字符="hello world!"
```
+ 之后我们使用str或字符就是`"hello world!"`了。

<Callout type="info" >
  想要了解变量声明在计算机底层发生过程的朋友可以看下去，萌新可以直接进入<Link href="./type" className="title">下一章</Link>了。
</Callout>
### 什么是变量声明
+ 实际上变量声明赋值就是我们在计算机内部开辟一个空间，把`hello world!`存进去。
+ 并且开辟和存储在计算机内部是两个过程。
```ts
let str="hello world!";
print str;
```
+ 程序会在内部执行`let str`即在计算机内存中开辟空间 并将返回的地址[^ps2]和`str`对应。
+ `str="hello world!"`则是在那个地址中放入`"hello world!"`;
+ 之后我们使用str 就意味着使用开辟的地址，便可以在其中取出"hello world!"了。
### 本章中英对照表
| 英文      | 中文 |
| ----------- | ----------- |
| print | 打印    |
|Ram| 这里指计算机内存条。|
|let | 允许、让或使。 这里作为变量声明的表示符号 |
|str| 字符串（一串字符）|
[^ps1]: *'hello,World!' 中文意思是“你好，世界”。
因为 在C语言 中使用它做为第一个演示程序，后来的程序员在学习编程或进行设备调试时延续了这一习惯。* 
[^ps2]: *程序中的地址一般指计算机内存(Ram)中的地址,内存即插在主板上的内存条，由<Link href="../talk/architecture" className="title">冯诺依曼架构</Link>中的存储器。
其返回的地址形式上为`0x102030`。*