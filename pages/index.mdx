import {useState} from "react"
import Link from "next/link"
export const Main = () => {
  const routes=[
    {
      title:"阅读文档 ↗",
      path:"./docs/pre"
    },{
      title:"安装",
      path:"./docs/install"
    },{
      title:"B站解释器视频",
      path:"./docs"
    }
  ]
  return (
      <div className="mt-20">
      <div className="flex-col">
        <div className="h-auto mt-4 mb-4">
              <span className="text-4xl font-bold title">
            汉语编程</span>
        </div>
                <div className="h-auto mt-4 mb-4">
        <span className="text-6xl font-bold bg-gradient-to-r from-pink-500 to-violet-500 bg-clip-text text-transparent">无障碍上手</span>
          </div>
                  <div className="h-auto mt-4 mb-4">
        <span className="text-6xl">比Python还要简单的编程语言</span>
          </div>
        </div>
         <div className="flex-row space-x-6 mt-8">
          {routes.map(r=><Link key={r.path} href={r.path}><button className="btn btn-outline">{r.title}</button></Link>)}
          </div>
      </div>
  );
};

<Main/>