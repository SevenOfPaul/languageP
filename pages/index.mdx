import { Github, Phone, MessageSquareMore, Mail } from 'lucide-react'
import Image from 'next/image';
import { motion } from 'framer-motion';
import ProfileCard from '../components/ProfileCard';
import { Card } from '../components/Card';

export const Main = () => {
  const categories = [
    {
      title: "前端",
      description: "",
      imageUrl: "/imgs/react.png",
      link: "/frontend"
    },
    {
      title: "rCore",
      description: "",
      imageUrl: "/imgs/rcore.png",
      link: "/rCore/desc"
    },
    {
      title: "解释器",
      description: "",
      imageUrl: "/imgs/interpreter.png",
      link: "/docs/p"
    },
    {
      title: "杂谈",
      description: "",
      imageUrl: "/imgs/talk.png",
      link: "/talk/architecture"
    }
  ];

  const container = {
    hidden: { opacity: 0 },
    show: {
      opacity: 1,
      transition: {
        staggerChildren: 0.15,
        delayChildren: 0.3
      }
    }
  };

  const cardVariants = {
    hidden: { 
      opacity: 0,
      y: 50,
      rotateX: -60,
      transformOrigin: "top"
    },
    show: { 
      opacity: 1,
      y: 0,
      rotateX: 0,
      transition: {
        type: "spring",
        bounce: 0.4,
        duration: 0.8
      }
    }
  };

  return (
    <motion.div
      initial="hidden"
      animate="show"
      variants={container}
    >
      <motion.div variants={container}>
        <ProfileCard/>
      </motion.div>

      <motion.div 
        variants={container}
        className="flex w-full flex-col md:flex-row justify-center gap-6 mt-12 md:px-4 px-0 pb-4"
      >
        {categories.map((category, index) => (
          <motion.div 
            key={category.title}
            variants={cardVariants}
            className="md:w-64 w-full flex-shrink-0"
          >
            <Card {...category} />
          </motion.div>
        ))}
      </motion.div>
    </motion.div>
  ) 
}

<Main/>


