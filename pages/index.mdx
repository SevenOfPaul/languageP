import {useState} from "react"
import Link from "next/link"
export const Main = () => {
  const routes=[
    {
      title:"阅读文档 ↗",
      path:"./docs/pre"
    },{
      title:"安装",
      path:"./docs/install"
    },{
      title:"B站解释器视频",
      path:"./docs"
    }, {
      title: "Flyingzzf",
      path: "https://zzf-gitch.github.io/vitepress-zzf"
    }
  ];
  const banner=[
  {  title:"好上手",desc:"支持中文关键字，让英语不再成为学习程序的阻碍。"},
  {  title:"性能还行",desc:"使用Rust语言编写底层代码，飞一般的感觉？好吧，飞的有点慢，毕竟是AST执行。"},
  {  title:"函数式",desc:"面向对象什么的，完全不care好吧！"}
  ]
  return (
      <div className="mt-20">
      <div className="flex-col">
        <div className="h-auto mt-4 mb-4">
              <span className="text-4xl font-bold title">
            汉语编程</span>
        </div>
                <div className="h-auto mt-4 mb-4">
        <span className="text-6xl font-bold bg-gradient-to-r from-pink-500 to-violet-500 bg-clip-text text-transparent">无障碍上手</span>
          </div>
                  <div className="h-auto mt-4 mb-4">
        <span className="text-6xl">比Python还要简单的编程语言</span>
          </div>
        </div>
         <div className="flex-row space-x-6 mt-8">
          {routes.map((r, idx) => (
            idx == routes.length - 1 ? (
              <div className="tooltip tooltip-bottom" data-tip="阿飞's blog">
                <Link key={r.path} href={r.path}>
                  <button className="btn btn-outline mt-1 mb-1">
                    {r.title}
                  </button>
                </Link>
              </div>
            ) : (
              <Link key={r.path} href={r.path}>
                <button className="btn btn-outline mt-1 mb-1">
                  {r.title}
                </button>
              </Link>
            )
          ))}
          </div>
          <hr className="mb-6 mt-6"/>
          <div className="flex flex-row justify-center mt-2 pt-2 flex-wrap">
            {banner.map(b=> <div key={b.title} className="flex-1">
              <p className="text-xl font-semibold mt-2 mb-2">{b.title}</p>
                <p className="w-36">{b.desc}</p>
              </div>)}
          </div>
      </div>
  );
};

<Main/>